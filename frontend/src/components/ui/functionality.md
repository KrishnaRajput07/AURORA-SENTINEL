# UI Utilities — Functionality

This directory contains reusable UI utility components.

## Key Files

### `background-boxes.jsx`
- Animated background component using Framer Motion
- Renders a grid of colored boxes with CSS transforms (skew, scale, rotate)
- Used on the landing page for visual flair
